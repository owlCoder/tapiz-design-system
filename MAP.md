# tapiz-design-system — Map

> Per-repo navigation map. Read this before exploring. See repo `CLAUDE.md`/`AGENTS.md` for rules.

## Structure

<!-- AUTO:START -->
_Generated by `scripts/gen-map.mjs` — do not edit between the markers; run the script._

- **Components** (13): `data-display`, `disclosure`, `feedback`, `forms`, `framework`, `icons`, `layout`, `marketing`, `modals`, `navigation`, `overlays`, `shared`, `table`
<!-- AUTO:END -->

## Where to look (manual — keep terse)

| Task | Start here |
|---|---|
| Brand tokens (Ink & Ember 2.0: palette, radii, shadows) | `src/theme.css` (`@theme` block; light overrides under `html:not(.dark)`) |
| Color skins (8: default/ocean/forest/rose/graphite/sand/crimson/aurora) | `src/skins.ts` (`TAPIZ_SKINS`/`TAPIZ_SKIN_IDS` metadata) + `html[data-skin]` blocks in `src/theme.css` |
| Buttons (`btn-primary`/`btn-secondary` utilities) | `src/theme.css` — consumers must not re-implement per app |
| Generic icons (lucide re-exports, curated Tapiz aliases) | `src/components/icons/` barrel — add a re-export, never hand-draw |
| Brand/domain glyphs (`LogoMark`, `Pdf`, store badges…) | `src/components/icons/BrandIcons.tsx` |
| New component | match the category folder under `src/components/` + export from the barrel |
| Publish | npm `@tapizlabs/ui` (current `2.2.1`); consumers: lms web, boards, cloud-status, sentinel on `^2.2.1`; whiteboard/specsgen still `^0.2.30` |
